import * as React from "react";
import DocsExampleView from "@/src/components/ExampleView/DocsExampleViewDynamic";

**Content work in progress. Please check back soon!**

## Prerequisites

Before you begin, make sure you have met the following prerequisites:

<DocsExampleView code={`<m-plane color="green" width="20" height="20" rx="-90"></m-plane>
  <m-light type="directional" ry="45" rx="45" debug="true" x="3" y="3" z="3"></m-light>
  <m-cube y="2" id="my-cube" color="red"></m-cube>
  <m-cube x="2" y="2" color="orange" visible-to="1"></m-cube>

  <script>
    let toggle = false;
    const myCube = document.getElementById("my-cube");
    if (window.params.color) {
    myCube.setAttribute("color", window.params.color);
  }
    myCube.addEventListener("click", () => {
    toggle = !toggle;
    myCube.setAttribute("color", toggle ? "blue" : "red");
  });
    setInterval(() => {
    myCube.setAttribute("ry", new Date().getTime() / 50);
  }, 500);
  </script>
`} />
